NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_GOOGLE_MAPS_KEY=
# Key separada para llamadas server-side (Distance Matrix). Sin restricción de HTTP referrer.
# Si está vacía, se cae a NEXT_PUBLIC_GOOGLE_MAPS_KEY (solo OK en local sin restricciones).
GOOGLE_MAPS_SERVER_KEY=
# Nunca commitear ni exponer al cliente. Solo server-side.
SUPABASE_SERVICE_ROLE_KEY=
